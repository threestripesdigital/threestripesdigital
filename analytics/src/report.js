import {posthogReport} from './posthog.js';
import {sourceReport} from './sources.js';
import {dayIn,shiftDay,ids} from './meta.js';
import {divide,score,learning,adMetrics} from './metrics.js';
export async function report(env,url) {
 const state=Object.fromEntries((await env.DB.prepare('SELECT * FROM state').all()).results.map(x=>[x.key,x.value]));
 const launchAt=env.LAUNCH_AT||state.launch_at;
 // Prelaunch reviews and previews must not become paid visitors or conversions.
 const paidFrom=Number.isFinite(Date.parse(launchAt))?new Date(launchAt).toISOString():'9999-12-31T23:59:59.999Z';

 const end=dayIn(Date.now(),env.REPORTING_TIMEZONE), days=[1,7,30,90].includes(Number(url.searchParams.get('days')))?Number(url.searchParams.get('days')):7;
 const start=shiftDay(end,1-days), selected=url.searchParams.get('campaign')||'';
 const allowed=ids({...env,META_CAMPAIGN_IDS:env.META_CAMPAIGN_IDS||state.meta_campaign_ids}), campaigns=selected?(allowed.includes(selected)?[selected]:[]):allowed;
 let websiteDays=[];try{websiteDays=JSON.parse(state.meta_website_actions||'[]');}catch{}
 const metaWebsite=state.meta_website_actions?websiteDays.filter(r=>r.day>=start&&r.day<=end&&campaigns.includes(r.campaign_id)).reduce((a,r)=>({landingPageViews:a.landingPageViews+r.landingPageViews,websiteLeads:a.websiteLeads+r.websiteLeads}),{landingPageViews:0,websiteLeads:0}):null;
 const placeholders=campaigns.length?campaigns.map(()=>'?').join(','):'NULL';
 const scope=`campaign_id IN (${placeholders})`;
 const meta=(await env.DB.prepare(`SELECT * FROM meta_daily WHERE day BETWEEN ? AND ? AND ${scope}`).bind(start,end,...campaigns).all()).results;
 const sessions=(await env.DB.prepare(`SELECT * FROM sessions WHERE day BETWEEN ? AND ? AND ${scope} AND started_at >= ?`).bind(start,end,...campaigns,paidFrom).all()).results;
 const calls=(await env.DB.prepare(`SELECT c.*,COALESCE(s.campaign_id,c.booking_campaign) campaign_id,s.ad_id,s.day session_day FROM calls c LEFT JOIN sessions s ON s.id=c.session_id WHERE ((s.${scope} AND s.started_at >= ?) OR (c.booking_source='meta' AND c.event_type_uri=? AND c.booked_at>=? ${selected?'AND c.booking_campaign=?':''})) AND (c.booked_day BETWEEN ? AND ? OR c.scheduled_day BETWEEN ? AND ? OR substr(c.paid_at,1,10) BETWEEN ? AND ? OR s.day BETWEEN ? AND ?)`).bind(...campaigns,paidFrom,env.CALENDLY_EVENT_TYPE_URI,paidFrom,...(selected?[selected]:[]),start,end,start,end,shiftDay(start,-1),shiftDay(end,1),start,end).all()).results;
 const counts=(await env.DB.prepare(`SELECT e.kind,COUNT(*) n FROM events e JOIN sessions s ON s.id=e.session_id WHERE s.day BETWEEN ? AND ? AND s.${scope} AND s.started_at >= ? GROUP BY e.kind`).bind(start,end,...campaigns,paidFrom).all()).results;
 const events=Object.fromEntries(counts.map(x=>[x.kind,x.n]));
 const revenue=(await env.DB.prepare(`SELECT COALESCE(SUM(r.amount),0) amount FROM revenue r JOIN calls c ON c.id=r.call_id JOIN sessions s ON s.id=c.session_id WHERE r.day BETWEEN ? AND ? AND s.${scope} AND s.started_at >= ?`).bind(start,end,...campaigns,paidFrom).first()).amount;
 const hold=learning(env.LAUNCH_AT||state.launch_at);
 const totals=meta.reduce((a,r)=>({spend:a.spend+r.spend,impressions:a.impressions+r.impressions,link_clicks:a.link_clicks+r.link_clicks}),{spend:0,impressions:0,link_clicks:0});
 const inRange=day=>day&&day>=start&&day<=end;
 const booked=calls.filter(c=>inRange(c.booked_day)&&c.status!=='cancelled');
 const cohort=calls.filter(c=>inRange(c.session_day)&&c.status!=='cancelled');
 const occurred=calls.filter(c=>inRange(c.scheduled_day)&&Date.parse(c.scheduled_at)<=Date.now()&&c.status!=='cancelled');
 const resolved=occurred.filter(c=>c.status==='showed'||c.status==='no_show');
 const showed=occurred.filter(c=>c.status==='showed');
 const paid=calls.filter(c=>c.paid_at&&inRange(dayIn(c.paid_at,env.REPORTING_TIMEZONE)));
 const boosted=showed.filter(c=>c.boosted_at);
 const assessed=booked.filter(c=>c.qualified!==null);
 const spend=totals.spend,qualified=booked.filter(c=>c.qualified===1).length;
 const cac=divide(spend,paid.length),avgRetainer=divide(paid.reduce((n,c)=>n+(c.monthly_retainer||0),0),paid.length);
 const values={...adMetrics(totals),play:divide(events.vsl_play||0,sessions.length,100),retention:divide(events.vsl_50||0,events.vsl_play||0,100),application:divide(events.application_complete||0,events.application_start||0,100),conversion:divide(new Set(cohort.map(c=>c.session_id)).size,sessions.length,100),scheduled_cost:divide(spend,booked.length),qualified_rate:divide(qualified,assessed.length,100),qualified_cost:divide(spend,qualified),showed_cost:divide(spend,showed.length),show:divide(showed.length,resolved.length,100),close:divide(showed.filter(c=>c.paid_at).length,showed.length,100),boost:divide(boosted.length,showed.length,100),boost_paid:divide(boosted.filter(c=>c.paid_at).length,boosted.length,100),cac,roas:divide(revenue,spend),payback:divide(cac,avgRetainer)};
 const metaBooked=(await env.DB.prepare(`SELECT COUNT(*) n FROM calls WHERE booking_source='meta' AND event_type_uri=? AND booked_at>=? AND booked_day BETWEEN ? AND ? AND status!='cancelled' ${selected?'AND booking_campaign=?':''}`).bind(env.CALENDLY_EVENT_TYPE_URI,paidFrom,start,end,...(selected?[selected]:[])).first()).n;
 const bookingsReady=!!state.funnel_success&&!state.funnel_error;
 values.scheduled_cost=bookingsReady?divide(spend,metaBooked):null;
 const samples={cpm:totals.impressions,ctr:totals.impressions,cpc:totals.link_clicks,play:sessions.length,retention:events.vsl_play||0,application:events.application_start||0,conversion:sessions.length,scheduled_cost:metaBooked,qualified_rate:assessed.length,qualified_cost:qualified,showed_cost:showed.length,show:resolved.length,close:showed.length,boost:showed.length,boost_paid:boosted.length,cac:paid.length,roas:paid.length,payback:paid.length};
 let adActions=[],formDays=[];try{adActions=JSON.parse(state.meta_ad_actions||'[]');formDays=JSON.parse(state.form_fill_daily||'[]');}catch{}
 adActions=adActions.filter(r=>inRange(r.day)&&campaigns.includes(r.campaign_id));formDays=formDays.filter(r=>inRange(r.day)&&(campaigns.includes(r.campaign_id)||(!selected&&!r.campaign_id)));
 const formsReady=!!state.form_fill_success&&!state.form_fill_error;
 const validForms=formDays.filter(r=>r.attributed&&meta.some(m=>m.ad_id===r.ad_id&&m.adset_id===r.adset_id&&m.campaign_id===r.campaign_id));
 const group=(key)=>Object.values(meta.reduce((a,r)=>{const k=r[key];const g=a[k]||{id:k,name:key==='ad_id'?r.ad_name:key==='adset_id'?(adActions.find(x=>x.adset_id===k)?.adset_name||k):r.campaign_name,campaign_id:r.campaign_id,spend:0,impressions:0,link_clicks:0};for(const f of ['spend','impressions','link_clicks'])g[f]+=r[f];a[k]=g;return a;},{})).map(g=>{const leads=state.meta_ad_actions?adActions.filter(r=>r[key]===g.id).reduce((n,r)=>n+r.websiteLeads,0):null;const forms=formsReady?validForms.filter(r=>r[key]===g.id).reduce((n,r)=>n+r.forms,0):null;return {...adMetrics(g),leads,cost_per_lead:leads>0?g.spend/leads:null,form_fills:forms,cost_per_form:forms>0?g.spend/forms:null};}).sort((a,b)=>b.spend-a.spend);

 const trend=Array.from({length:days},(_,i)=>{const day=shiftDay(start,i),rows=meta.filter(r=>r.day===day);return {day,...adMetrics(rows.reduce((a,r)=>({spend:a.spend+r.spend,impressions:a.impressions+r.impressions,link_clicks:a.link_clicks+r.link_clicks}),{spend:0,impressions:0,link_clicks:0}))};});
 const stale=!state.meta_success||Date.now()-Date.parse(state.meta_success)>2*3600000;
 const browserTracking=env.BROWSER_TRACKING_ENABLED!=='false';
 const sources=sourceReport(env,state,days,end);
 sources.posthog=posthogReport(env,state,start,end,campaigns);
 const ph=sources.posthog;
 const videoRows=sources.wistia.window?.attribution?.rows||[];
 const withVideo=(rows,key)=>rows.map(g=>{if(ph.connected&&!ph.stale){const matches=ph.rows.filter(r=>r[key]===g.id);const loads=matches.reduce((n,r)=>n+r.vsl_load,0),plays=matches.reduce((n,r)=>n+r.vsl_play,0);if(loads>0)return {...g,vsl_loads:loads,vsl_plays:plays,vsl_play_rate:divide(plays,loads,100),cost_per_vsl_play:null,vsl_source:"PostHog, since Sep 12"};}const matches=videoRows.filter(r=>r[key]===g.id&&campaigns.includes(r.campaign_id));const usable=sources.wistia.connected&&sources.wistia.window?.attribution?.complete&&matches.length>0;const loads=usable?matches.reduce((n,r)=>n+r.loads,0):null,plays=usable?matches.reduce((n,r)=>n+r.plays,0):null;return {...g,vsl_loads:loads,vsl_plays:plays,vsl_play_rate:divide(plays,loads,100),cost_per_vsl_play:divide(g.spend,plays)};});
 const alerts=[];
 if(!browserTracking)alerts.push({level:'ok',title:'Direct source reporting',text:'Custom browser collection is off. Provider reports appear separately. Calendly supplies booked-call attribution; Wistia and PostHog supply video metrics. Each metric uses its available source.'});
 if(state.ga4_error)alerts.push({level:'bad',title:'Google Analytics needs attention',text:state.ga4_error});
 if(state.meta_error) alerts.push({level:'bad',title:'Meta connection needs attention',text:state.meta_error});
 if(stale) alerts.push({level:'ok',title:'Ad data is not current',text:state.meta_success?'The last successful sync is over two hours old. Resolve the connection before making decisions.':'No successful Meta sync yet. No performance conclusions are available.'});
 if(state.funnel_error) alerts.push({level:'bad',title:'Booking connection needs attention',text:state.funnel_error});
 if(browserTracking&&totals.link_clicks>=20&&!sessions.length) alerts.push({level:'bad',title:'Check tracking now',text:'Meta reports at least 20 link clicks but no attributed visits arrived. Check campaign URL parameters and the funnel analytics endpoint.'});
 if(values.cpm>=80&&totals.impressions>=1000) alerts.push({level:'bad',title:'Extreme CPM: investigate',text:'CPM is $80 or higher with at least 1,000 impressions. Check delivery and creative relevance now. No automatic campaign changes are made.'});
 if(browserTracking&&!hold.locked&&booked.length===0&&spend>=700) alerts.push({level:'bad',title:'No booked calls after baseline spend',text:'Inspect the VSL, application and scheduler. Confirm tracking before diagnosing conversion.'});
 if(occurred.length>resolved.length) alerts.push({level:'ok',title:'Call outcomes are missing',text:`${occurred.length-resolved.length} past calls need a showed or no-show outcome. Show rate excludes unresolved calls.`});
 if(booked.length>assessed.length)alerts.push({level:'ok',title:'Qualification needs review',text:`${booked.length-assessed.length} booked calls have unknown sales qualification. Qualified rate excludes unknowns; qualified cost is provisional.`});
 const scored=score(values,samples,hold).map(m=>({...m,recommendation:m.value===null?({retention:'Requires VSL plays and watched-half events from PostHog. Recording starts Sep 12.',application:'Requires form-start and completed-scan events from PostHog.',conversion:'Requires individually linked visit and verified booking records. The separate landing-view booking ratio is available.',scheduled_cost:'No verified Meta-source bookings in this period; cost per booking has no denominator.',roas:'Requires a reconciled revenue source. Unrecorded revenue cannot be assumed to be zero.',payback:'Requires paid clients with recorded monthly retainers.'}[m.key]||m.recommendation):m.recommendation,provisional:((m.key==='show'||m.key==='showed_cost')&&occurred.length>resolved.length)||(['qualified_rate','qualified_cost'].includes(m.key)&&booked.length>assessed.length)}));
 const callList=(await env.DB.prepare(`SELECT c.*,s.ad_id FROM calls c LEFT JOIN sessions s ON s.id=c.session_id WHERE (c.booked_day BETWEEN ? AND ? OR c.scheduled_day BETWEEN ? AND ?) ${selected?`AND s.${scope}`:''} ORDER BY (c.status='scheduled' AND c.scheduled_at<=?) DESC,c.scheduled_at DESC LIMIT 100`).bind(start,end,start,end,...(selected?campaigns:[]),new Date().toISOString()).all()).results;
 const allCampaigns=(await env.DB.prepare('SELECT DISTINCT campaign_id id,campaign_name name FROM meta_daily ORDER BY campaign_name').all()).results.filter(c=>allowed.includes(c.id));
 const payingClients=(await env.DB.prepare(`SELECT c.id,c.tier,c.monthly_retainer FROM calls c JOIN sessions s ON s.id=c.session_id WHERE c.paid_at IS NOT NULL AND s.${scope} AND s.started_at >= ? ORDER BY c.paid_at DESC LIMIT 2000`).bind(...campaigns,paidFrom).all()).results;
 const unlinked=(await env.DB.prepare('SELECT COUNT(*) n FROM calls WHERE session_id IS NULL').first()).n;
 return {metaBookings:{count:bookingsReady?metaBooked:null,source:"Calendly · Rank Boost · source=meta",ready:bookingsReady},metaWebsite,sources,period:{start,end,days,timezone:env.REPORTING_TIMEZONE,currency:env.CURRENCY},hold,state:Object.fromEntries(Object.entries(state).filter(([k])=>!k.startsWith('creative_')&&!k.startsWith('meta_preview:')&&!['meta_ad_actions','form_fill_daily','posthog_daily'].includes(k))),stale,alerts,totals:{...totals,visitors:sessions.length,booked:booked.length,showed:showed.length,paid:paid.length,revenue,unlinked,qualified,qualificationUnknown:booked.length-assessed.length,attendanceUnknown:occurred.length-resolved.length},metrics:browserTracking?scored:scored.map((m,i)=>i<3||['scheduled_cost','qualified_rate','qualified_cost','showed_cost','show','close','boost','boost_paid','cac','payback'].includes(m.key)&&bookingsReady?m:{...m,value:null,sample:0,status:'waiting',recommendation: m.recommendation}),campaigns:allCampaigns,campaignRows:withVideo(group('campaign_id'),'campaign_id'),ads:withVideo(group('ad_id'),'ad_id'),adsets:withVideo(group('adset_id'),'adset_id'),formFillReporting:{total:formsReady?formDays.reduce((n,r)=>n+r.forms,0):null,qualified:formsReady?formDays.reduce((n,r)=>n+r.qualified,0):null,ready:formsReady,error:state.form_fill_error||'',unattributed:formsReady?formDays.filter(r=>!validForms.includes(r)).reduce((n,r)=>n+r.forms,0):null},trend,events,payingClients,calls:callList,setup:{campaignPrefix:env.META_CAMPAIGN_PREFIX||'Rank Boost',meta:!!env.META_ACCESS_TOKEN,account:!!env.META_ACCOUNT_ID,campaigns:allowed.length,bookings:!!state.funnel_success,events:browserTracking&&!!state.event_success},budget:Number(env.DAILY_BUDGET),notes:[...(!Number.isFinite(Date.parse(launchAt))?['Campaign has not launched. Prelaunch visits and conversions are excluded from paid-performance totals.']:[]),...(env.META_ACCOUNT_CURRENCY&&env.META_ACCOUNT_CURRENCY!==env.CURRENCY?[`Meta bills in ${env.META_ACCOUNT_CURRENCY}. Spend is shown in ${env.CURRENCY} using the fixed reference rate of 1 USD = ${env.META_USD_TO_ACCOUNT_RATE} ${env.META_ACCOUNT_CURRENCY} dated ${env.META_FX_DATE}. This is an estimate, not a live exchange rate or your card settlement amount.`]:[]),'Thresholds are targets from your supplied strategy, not promises of performance.','OK is the band between good and bad. The displayed midpoint is a reference, not a separate cutoff.','Meta link-click metrics are separate from first-party session and verified booking counts. Browser events can be blocked or spoofed.','VSL retention is sessions that watched at least 50% of the video divided by sessions that played. Seeking alone does not qualify. Organic sessions are excluded.', 'Qualified rate is manually qualified bookings divided by assessed bookings created in the period. Unknowns are excluded and flagged. Keyword eligibility is separate from sales qualification.', 'Show rate uses resolved calls scheduled in this period. Future and unresolved calls are excluded.','Visitor conversion follows sessions started in this period. Late bookings can change past cohorts.','CAC uses new paid clients in the period. ROAS uses collected revenue, including refunds, in the period. Use 90 days for the strategy comparison.','Revenue payback excludes margin and operating costs. These figures do not establish profitability.','Calls without tracked campaign attribution are excluded from paid-ad performance.']};
}
