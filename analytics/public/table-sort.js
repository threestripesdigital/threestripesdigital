export function sortRows(rows,key,direction='desc'){const sign=direction==='asc'?1:-1;return [...rows].sort((a,b)=>{const x=a[key],y=b[key],missing=v=>v==null||(typeof v==='number'&&!Number.isFinite(v));if(missing(x))return missing(y)?String(a.id).localeCompare(String(b.id)):1;if(missing(y))return -1;return (typeof x==='string'?x.localeCompare(String(y)):x-y)*sign||String(a.id).localeCompare(String(b.id));});}
